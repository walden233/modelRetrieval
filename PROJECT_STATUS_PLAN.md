[cite_start]这份关于“通用场景下机器人动作模型的多模态检索方法研究”的开题报告展示了一个从理论研究到工业应用的完整闭环 [cite: 208]。以下是基于该报告整理的详细任务规划，涵盖了从数据准备到论文撰写的全过程。

---

## 1. 第一阶段：数据准备与初步预处理 (2025.10.20 前)
[cite_start]本阶段的核心是构建训练多模态编码器所需的基础数据集 [cite: 198]。

* [cite_start]**数据集整理**：使用 **RH20T**（包含超过 11 万个真实机器人操作序列）和 **HUMAN2ROBOT** 等公开数据集 [cite: 79, 198]。
* [cite_start]**样本配对**：提取并整理“人类演示视频”与“机器人执行视频”的配对数据 [cite: 198]。
* [cite_start]**视频预处理**：完成视频解码、关键帧采样以及尺寸归一化等操作 [cite: 198]。
* [cite_start]**轨迹数据提取**：利用 **MediaPipe**、**HRNet** 或 **OpenPose** 等算法，从人类演示视频中逐帧提取手部关节点（2D/3D）时序轨迹，并进行数据清洗 [cite: 127, 203]。

## 2. 第二阶段：语义层面的 VLM 模块开发 (2025.11.20 前)
[cite_start]利用大模型的零样本能力，为检索系统提供高维度的语义支撑 [cite: 136, 137]。

* [cite_start]**VLM 选型与封装**：调研并接入 **GLM-4.5** 等大型视觉语言模型，设计健壮的 API 调用模块 [cite: 203]。
* [cite_start]**提示词工程 (Prompt Engineering)**：优化用于零样本标签分类（如抓取、放置、装配能力）和任务描述生成的 Prompt 模板 [cite: 142, 146, 203]。
* [cite_start]**特征编码化**：调用第三方文本编码模型，将生成的任务描述和标签转化为高维文本特征向量 [cite: 147, 203]。
* [cite_start]**初步验证**：手动构建 50-100 个样本的小型验证集，评估 VLM 生成标签的精确率和召回率 [cite: 149]。

## 3. 第三阶段：跨领域多模态编码器训练 (2026.04.15 前)
[cite_start]这是研究的技术核心，旨在弥合人与机器人之间的“领域鸿沟” [cite: 33, 114]。

* **跨领域视频编码器 ($E_{vid}$)**：
    * [cite_start]**架构**：基于 Transformer 的骨干网络（如 VideoMoCo） [cite: 118, 119]。
    * [cite_start]**训练**：采用 **InfoNCE 损失函数** 进行对比学习，拉近同任务的人-机视频特征，推远异任务视频 [cite: 119]。
    * [cite_start]**目标**：通过余弦相似度准确衡量人机视频的任务相关性 [cite: 117]。
* **跨领域轨迹编码器 ($E_{traj}$)**：
    * [cite_start]**架构**：采用 Transformer Encoder 处理变长的手部/末端执行器轨迹序列 [cite: 128, 203]。
    * [cite_start]**训练**：将人类手部轨迹与机器人执行轨迹作为正样本对进行训练 [cite: 131]。

## 4. 第四阶段：检索系统开发与集成 (2026.05.10 前)
[cite_start]将算法模型工程化，构建端到端的检索服务 [cite: 163]。

* **离线索引流水线**：
    * [cite_start]对模型仓库中的所有机器人动作模型进行批量特征提取（视频、轨迹、文本、标签） [cite: 166]。
    * [cite_start]利用 **FAISS** 向量数据库存储高维特征并建立近似最近邻搜索索引 [cite: 167, 191, 203]。
* **在线查询引擎**：
    * [cite_start]实现四路并行特征提取与检索（视频、轨迹、文本、标签） [cite: 170, 237]。
    * [cite_start]**多模态融合**：通过加权求和公式计算综合相似度得分，并利用网格搜索优化各模态权重 [cite: 174, 180]。
* [cite_start]**系统部署**：基于 Flask 框架和 Docker 容器技术，开发 RESTful API 接口，实现与 **insightOS** 模型仓库的对接 [cite: 203]。

## 5. 第五阶段：实验验证、优化与论文产出 (2026.11.20 前)
* [cite_start]**性能评估**：在 RH20T 测试集上评估 **Recall@K**、**MRR** 和 **NDCG** 等核心指标 [cite: 153, 154, 155, 203]。
* [cite_start]**消融实验**：通过逐一移除视频、轨迹、标签等模态，量化各模块对检索准确性的贡献 [cite: 160, 203]。
* [cite_start]**生产环境验证**：将系统实际接入 insightOS 平台，验证其在真实任务部署中的效率提升情况 [cite: 223]。
* **撰写与答辩**：
    * [cite_start]2026.07.15：完成中期答辩报告 [cite: 223]。
    * [cite_start]2026.11.20：完成最终毕业论文撰写及答辩准备 [cite: 223]。

---

### 任务规划关键时间表摘要

| 时间节点 | 主要任务 | 预期成果 |
| :--- | :--- | :--- |
| **2025.10** | 数据处理与轨迹提取 | [cite_start]整理完成的配对数据集 [cite: 219] |
| **2025.11** | VLM 语义匹配开发 | [cite_start]标签化与描述生成功能 [cite: 219] |
| **2025.12** | Baseline 搭建 | [cite_start]基础多模态检索系统原型 [cite: 219] |
| **2026.04** | 核心编码器训练 | [cite_start]高性能视频与轨迹编码模型 [cite: 223] |
| **2026.06** | 实验验证 | [cite_start]消融实验数据与结果分析 [cite: 223] |
| **2026.09** | insightOS 接入 | [cite_start]生产环境验证报告 [cite: 223] |
| **2026.11** | 论文定稿 | [cite_start]高质量硕士毕业论文 [cite: 223] |

# 通用场景下机器人动作模型多模态检索项目现状与完整实现规划

更新时间：2026-04-07

本文档基于当前仓库代码、目录结构、已存在数据与模型文件整理，目标是回答三个问题：

1. 当前项目已经做到哪一步。
2. 与开题报告相比还缺哪些关键部分。
3. 接下来应如何把现有研究原型推进为完整的多模态检索系统。

## 1. 结论概览

当前仓库已经完成了一个较完整的“数据准备 -> 轨迹建模 -> 检索评估”研究原型，尤其是跨领域轨迹编码器这一主线已经基本成型；视频侧已经有 baseline 和 finetune 原型；数据子集抽取、人手关键点提取、轨迹增强、超参数搜索、检索指标评估也都有实现。

但从开题报告的完整目标来看，当前项目仍然主要停留在“研究验证阶段”，距离“多模态检索系统闭环”还有四块明显缺口：

1. 语义侧缺失：尚未实现 VLM 标签生成、任务描述生成、文本编码和标签评估。
2. 系统侧缺失：尚未实现 FAISS 离线索引、在线多路检索、模态融合、RESTful API。
3. 工程侧不完整：缺少配置化、测试、依赖补全、统一实验管理。
4. 论文侧材料不足：当前仓库没有沉淀完整的实验结果、消融表、生产验证记录。

## 2. 当前仓库已完成内容

### 2.1 数据准备与预处理

这一阶段已经完成度较高。

已完成事项：

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| RH20T 子集抽取 | 已实现 | `src/utils/extract_rh20t_subset.py` 可从原始 RH20T 中抽取任务、场景、相机视角并重组目录。 |
| RH20T 轨迹数据组织 | 已实现 | `src/data/rh20t.py` 已支持扫描 `task_x/scene_y` 结构，加载 `human_pose.npy` 与 `tcp_base.npy`。 |
| 手部关键点提取 | 已实现 | `src/utils/extract_hand_pose.py` 基于 MediaPipe 逐帧提取 `human_pose.npy`。 |
| 手部关键点验证 | 已实现 | `src/utils/handpose_verify.py` 可检查轨迹长度、帧数、关键点格式。 |
| TCP 数据检查 | 已实现 | `src/utils/tcp_base_verify.py` 可检查 `tcp_base.npy` 结构。 |
| 数据批处理与变长对齐 | 已实现 | `collate_trajectories` 已支持 padding、mask 生成、机器人长序列动态降采样。 |
| 6 关键点版本 | 已实现 | `RH20TTraceDataset` 已支持从 21 个关键点降采样到 6 个关键点。 |

当前可确认的数据资产：

| 数据集 | 当前仓库状态 |
| :--- | :--- |
| `dataset/RH20T_subset/RH20T_cfg2` | 已存在，统计到 109 个 task、434 个 scene。 |
| `dataset/RH20T_subset/RH20T_cfg3` | 已存在，统计到 64 个 task、256 个 scene。 |
| `dataset/whirl_videos` | 已存在，human/robot 各 20 个视频，且包含 `dataset.csv`。 |

说明：

- RH20T 子集目录中已经实际存在 `*_human.mp4`、`*_robot.mp4`、`human_pose.npy` 和 `tcp_base.npy`，说明数据抽取与关键点提取已经不是停留在脚本阶段，而是已经实际跑过。
- 当前关键点抽取只看到 MediaPipe 方案，没有看到 HRNet 或 OpenPose 的实现。

### 2.2 视频与轨迹数据集封装

这一部分已形成训练可用的数据接口。

已完成事项：

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| RH20T 视频数据集 | 已实现 | `RH20TVideoDataset` 支持按 task 采样 scene 和 camera，输出人机视频对。 |
| RH20T 轨迹数据集 | 已实现 | `RH20TTraceDataset` 支持按 scene 加载人手轨迹和 TCP 轨迹。 |
| WHIRL 视频数据集 | 已实现 | `src/data/whirlDataset.py` 支持基于 CSV 的人机视频对读取。 |
| CSV 生成工具 | 已实现 | `src/utils/csv_generator.py` 可生成 human/robot 视频配对 CSV。 |

### 2.3 跨领域轨迹编码器训练

这是当前仓库最成熟的部分。

已完成事项：

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| 轨迹 Transformer 编码器 | 已实现 | `src/models/trajectoryEncoder.py` 已实现人类轨迹编码器、机器人轨迹编码器、位置编码、CLS token。 |
| 跨模态投影头 | 已实现 | 人类轨迹与机器人轨迹共享投影头并做 L2 归一化。 |
| 跨模态对比损失 | 已实现 | `trajectory_symmetric_contrastive_loss` 已支持 scene/task 多正样本软标签训练。 |
| 模态内对比损失 | 已实现 | `intra_modal_contrastive_loss` 已支持增强视图对比。 |
| 基础训练脚本 | 已实现 | `src/pipelines/trajectoryTrain.py`。 |
| 超参数搜索 | 已实现 | `src/pipelines/trajectory_hypersearch.py`。 |
| cfg2 -> cfg3 迁移训练 | 已实现 | `src/pipelines/trajectoryTrain_cfg23.py`。 |
| 轨迹增强训练 | 已实现 | `src/pipelines/trajectory_augment.py`。 |
| 两阶段训练 | 已实现 | `src/pipelines/trajectory_augment_2stage.py`。 |

当前可确认的实验产物：

| 文件 | 说明 |
| :--- | :--- |
| `model_weight/best_trajectory_model.pth` | 已存在，约 9.8 MB。 |
| `model_weight/best_trajectory_model_overall.pth` | 已存在，约 29 MB。 |
| `model_weight/best_model_params.json` | 已保存一组超参搜索配置。 |

说明：

- 这表明轨迹主线至少已经训练并保存过模型。
- 但 `results/` 目录当前为空，说明按 run 保存的曲线和指标没有沉淀到仓库中，当前只能确认“模型和参数文件存在”，无法直接从仓库恢复完整实验结论。

### 2.4 视频侧 baseline 与原型

视频侧属于“已开始，但尚未稳定”的状态。

已完成事项：

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| VideoMAE / V-JEPA 原型实验 | 已实现 | `scripts/videomae1.py`、`scripts/videomae2.py` 为探索性实验脚本。 |
| 视频特征投影头 | 已实现 | `src/models/finetuner.py` 包含 `VideomaeFineTuner` 和 `vjepaFineTuner`。 |
| 视频 baseline 检索评估 | 已实现 | `src/evaluation/base_line.py` 可基于 RH20TVideoDataset 做检索指标评估。 |
| 视频训练原型 | 部分实现 | `src/pipelines/videoFineTune.py` 表达了训练思路，但当前实现不够稳定。 |

当前判断：

- 视频主线还没有像轨迹主线那样形成稳定的“可复现实验链路”。
- 更像是已经完成 backbone 选型和 baseline 验证，但还没有统一纳入完整的多模态检索框架。

### 2.5 评估指标与结果保存

已完成事项：

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| 检索评估函数 | 已实现 | `src/evaluation/trajectory_functions.py` 支持 Recall@1/5/10、Mean Rank、MRR、Mean Percentage Rank。 |
| 视频检索评估函数 | 已实现 | `src/evaluation/functions.py` 支持基础 retrieval metrics。 |
| 结果保存工具 | 已实现 | `src/utils/save_results.py` 支持参数、最佳指标、训练曲线保存。 |

说明：

- 离线研究评估已经具备基础能力。
- 尚未看到 NDCG 的实现，也没有系统化 ablation 输出脚本。

## 3. 与开题报告五阶段对照后的完成度判断

### 第一阶段：数据准备与初步预处理

状态：大体完成

已完成：

- RH20T 子集抽取。
- 人机视频配对组织。
- 手部关键点提取与清洗。
- 人手轨迹和 TCP 轨迹的统一读取。
- 变长序列 padding、mask、降采样。

未完成或不充分：

- HUMAN2ROBOT 数据集尚未在仓库中看到接入痕迹。
- 未看到 HRNet / OpenPose 分支实现与效果对比。
- 预处理流程仍以脚本为主，缺少统一 CLI / config 管理。

### 第二阶段：语义层面的 VLM 模块开发

状态：基本未开始

当前仓库未看到以下内容：

- GLM-4.5 或其他 VLM 的接入封装。
- Prompt 模板管理。
- 标签生成与任务描述生成。
- 第三方文本编码器。
- 标签精确率 / 召回率验证集。

这是当前与开题计划之间最大的缺口之一。

### 第三阶段：跨领域多模态编码器训练

状态：部分完成，轨迹强、视频弱、文本缺失

已完成：

- 轨迹编码器主线已经完成。
- 跨模态对比训练、增强训练、两阶段训练都已实现。
- 视频侧有 baseline 和初步 finetune 探索。

未完成：

- 没有统一的视频编码器训练主线。
- 没有文本编码器、标签编码器。
- 没有“视频 + 轨迹 + 文本 + 标签”统一训练或统一嵌入空间。
- 论文中提到的多模态编码器，目前仓库更准确地说是“轨迹主导的双模态研究原型”。

### 第四阶段：检索系统开发与集成

状态：未开始

当前仓库未看到以下内容：

- FAISS 索引构建。
- 离线特征批量抽取流水线。
- 在线查询引擎。
- 多模态加权融合与网格搜索。
- Flask 服务。
- Docker 部署文件。
- insightOS 接口层。

### 第五阶段：实验验证、优化与论文产出

状态：部分完成

已完成：

- 已有基础检索指标评估函数。
- 已有超参数搜索和训练变体。
- 已有模型权重文件。

未完成：

- 仓库中没有系统化的实验报表、消融实验表格、最终结果汇总。
- 没有 NDCG 指标实现。
- 没有生产环境验证脚本。
- 没有中期答辩 / 论文写作材料沉淀目录。

## 4. 当前项目真正的阶段定位

如果用一句话描述当前项目状态：

当前项目已经完成了“跨领域轨迹检索研究原型”的核心验证，并搭好了视频分支的初步实验框架，但离“面向 insightOS 的完整四模态检索系统”还有明显距离。

因此，当前最准确的阶段定位应是：

> 已完成阶段 1，阶段 3 中的轨迹主线基本完成，视频主线处于 baseline/原型阶段；阶段 2、阶段 4 和阶段 5 仍需系统推进。

## 5. 当前仓库存在的主要问题

这些问题不代表方向错误，但会直接影响后续系统化推进。

### 5.1 工程化问题

| 问题 | 影响 |
| :--- | :--- |
| 大量脚本硬编码 `/home/ttt/BISE/...` 路径 | 可移植性差，不利于复现和部署。 |
| `setup.py` 依赖不完整 | 代码中使用了 `kornia`，但依赖里未声明。 |
| 缺少 `tests/` 目录 | 核心数据流程和评估逻辑没有自动化回归测试。 |
| `results/` 目录为空 | 训练过程和实验记录未沉淀，论文复现风险较高。 |
| 多数入口脚本未配置化 | 难以统一开展实验和对比。 |

### 5.2 研究实现问题

| 问题 | 影响 |
| :--- | :--- |
| 当前没有语义模态 | 无法实现开题报告中的四路检索。 |
| 视频训练主线不稳定 | 视频模态很难进入正式融合阶段。 |
| 尚未构建统一 embedding schema | 后续索引、融合、服务接口难以统一。 |
| 缺少 NDCG / 消融自动化 | 论文阶段会反复手工整理。 |

### 5.3 一个需要尽快修正的具体点

`src/pipelines/videoFineTune.py` 与 `src/models/finetuner.py` 当前接口并不完全匹配，说明视频训练脚本还没有真正收敛为稳定入口，后续需要优先重构。

## 6. 接下来完整实现思路

下面给出建议的完整推进路线。原则是：

1. 先把现有研究原型稳定下来。
2. 再补语义模态。
3. 然后做离线索引和在线检索。
4. 最后集中补实验、部署和论文材料。

## 6.1 第一优先级：先把现有代码整理成“可稳定复现”的研究框架

这一阶段建议先不急着上 Flask 或 FAISS，而是先做底座收敛。

建议新增目录：

```text
configs/
tests/
src/semantic/
src/retrieval/
src/service/
docs/
```

建议优先完成：

1. 把所有训练脚本改成配置驱动。
2. 去掉硬编码绝对路径，统一使用 `--data-root`、`--output-dir`、`--config`。
3. 补齐 `setup.py` 或迁移到 `pyproject.toml`，补入 `kornia`、`faiss-cpu/faiss-gpu`、`flask` 等依赖。
4. 为数据集读取、评估函数、损失函数补 `pytest`。
5. 统一结果目录结构，确保每次实验都保存：
   - 参数
   - 最佳指标
   - 曲线图
   - 预测结果
   - 错误案例

预期交付物：

- 稳定可运行的 `train_trajectory.py`
- 稳定可运行的 `train_video.py`
- 统一的 `evaluate_retrieval.py`
- 可复现的结果目录

## 6.2 第二优先级：补齐语义模态

这是当前与开题目标差距最大的部分，必须尽快补上。

建议实现：

### 6.2.1 VLM 封装层

在 `src/semantic/` 中新增：

- `vlm_client.py`：封装 GLM-4.5 或其他 VLM 调用。
- `prompts.py`：统一管理标签分类、任务描述生成 prompt。
- `schemas.py`：约束 VLM 输出 JSON 格式。
- `cache.py`：缓存调用结果，避免重复请求。

### 6.2.2 语义产物定义

每个样本最终应补齐以下字段：

- `task_label`
- `task_description`
- `capability_tags`
- `text_embedding`
- `label_embedding`

### 6.2.3 小规模人工验证集

建立 50 到 100 条人工标注样本，评估：

- 标签精确率
- 标签召回率
- 描述一致性
- 语义检索效果

预期交付物：

- 一份带缓存的 VLM 标注流水线
- 一份语义验证集
- 一份 VLM 标注效果报告

## 6.3 第三优先级：统一多模态特征空间

当前需要把“轨迹原型 + 视频原型 + 语义模态”整合成真正的多模态检索模型。

建议做法：

### 6.3.1 不建议一开始就做端到端四模态联合训练

更稳妥的实现顺序是：

1. 分别得到稳定的视频 embedding、轨迹 embedding、文本 embedding、标签 embedding。
2. 先做离线 late fusion。
3. 确认有效后，再决定是否做 joint training。

原因：

- 当前视频与语义分支还不够稳定。
- 先做 late fusion 更容易定位问题、产出论文实验。

### 6.3.2 统一样本表示

建议定义统一的样本特征结构，例如：

```json
{
  "sample_id": "...",
  "task_id": "...",
  "scene_id": "...",
  "video_embedding": [],
  "trajectory_embedding": [],
  "text_embedding": [],
  "label_embedding": [],
  "metadata": {}
}
```

### 6.3.3 融合策略

先做：

- 归一化后的加权求和
- 网格搜索权重

再做：

- 学习式融合
- query-aware 动态权重

预期交付物：

- 多模态样本 schema
- 融合权重搜索脚本
- 单模态 / 双模态 / 四模态对比实验表

## 6.4 第四优先级：实现检索系统

当四种特征都能稳定生成后，再进入系统化阶段。

建议拆成两部分。

### 6.4.1 离线索引流水线

新增 `src/retrieval/`：

- `extract_features.py`：批量抽取视频、轨迹、文本、标签特征。
- `build_index.py`：构建 FAISS 索引。
- `feature_store.py`：管理特征文件和元数据。
- `fusion.py`：多模态得分融合。

离线阶段输出：

- 特征文件
- FAISS 索引文件
- 样本元数据映射表

### 6.4.2 在线查询服务

新增 `src/service/`：

- `app.py`：Flask API 入口。
- `query_pipeline.py`：在线四路特征提取。
- `ranking.py`：融合、重排、结果返回。
- `schemas.py`：请求与响应定义。

核心 API 建议：

- `POST /index/rebuild`
- `POST /query/video`
- `POST /query/trajectory`
- `POST /query/multimodal`
- `GET /health`

预期交付物：

- 可离线重建索引
- 可在线检索
- 可返回 TopK 结果和各模态得分明细

## 6.5 第五优先级：实验验证与论文材料沉淀

这一阶段的目标不是再加功能，而是把结果变成论文能直接使用的材料。

建议补齐：

1. 标准指标：
   - Recall@1/5/10
   - MRR
   - NDCG
2. 消融实验：
   - 仅视频
   - 仅轨迹
   - 仅文本
   - 仅标签
   - 视频+轨迹
   - 视频+轨迹+文本
   - 全模态
3. 稳定性实验：
   - 不同关键点方案
   - 不同 backbone
   - 不同融合权重
4. 误差分析：
   - 检索失败案例
   - 跨任务混淆案例
   - 视角变化敏感案例
5. 生产验证：
   - insightOS 接入耗时
   - 检索时延
   - 模型复用效率提升

预期交付物：

- `results/final_tables/`
- `results/ablation/`
- `results/case_studies/`
- 论文图表与附录素材

## 7. 我接下来建议的实际执行顺序

如果从现在开始继续推进，我建议按下面顺序实施，而不是并行摊大饼。

### Step 1：先做代码基线收敛

目标：

- 修复视频训练入口。
- 把轨迹训练与评估改为配置化。
- 补自动化测试。
- 补依赖。

这样做的原因：

- 现有最强资产是轨迹链路，必须先稳住。
- 如果底层接口不统一，后面接语义模态和索引服务会越来越乱。

### Step 2：补语义生成链路

目标：

- 跑通 VLM 标签与描述生成。
- 落盘文本 embedding 和标签 embedding。
- 做小规模人工验证。

这是进入“四模态检索”的必要前提。

### Step 3：做多模态离线检索

目标：

- 不先上服务，先离线做四模态检索评估。
- 跑出单模态、双模态、全模态结果表。
- 完成融合权重搜索。

这样最容易快速产出论文结果。

### Step 4：做 FAISS 与服务化

目标：

- 实现 offline index + online query。
- 暴露 Flask API。
- 预留 insightOS 接口适配。

### Step 5：集中补论文材料与生产验证

目标：

- 做消融、误差分析、案例可视化、部署评估。
- 输出中期答辩和论文正文可直接引用的图表。

## 8. 推荐的近期里程碑

建议把接下来工作拆成三个短里程碑。

### 里程碑 M1：研究原型固化

目标：

- 统一训练入口
- 补测试
- 补结果保存
- 跑通稳定的视频与轨迹 baseline

交付：

- 可复现的轨迹结果
- 可复现的视频结果
- 规范化实验目录

### 里程碑 M2：四模态离线检索

目标：

- 接入 VLM
- 生成文本与标签特征
- 跑通融合检索与消融

交付：

- 四模态离线检索表
- 权重搜索结果
- 语义模态质量验证

### 里程碑 M3：系统与论文闭环

目标：

- FAISS 索引
- Flask API
- insightOS 对接
- 部署与论文材料

交付：

- 可调用的检索服务
- 生产验证报告
- 论文图表与结论材料

## 9. 当前状态总结

基于仓库现状，当前项目并不是“从零开始”，而是已经完成了最关键的一段研究积累：

- RH20T 子集数据已经准备好。
- MediaPipe 关键点抽取已经跑通。
- 轨迹编码器训练、增强训练、超参数搜索已经成型。
- 检索评估指标已经具备。
- 最佳模型权重已经存在。

但同时也必须清楚：

- 当前完成的是“轨迹驱动的研究原型”，不是“完整多模态检索系统”。
- 开题报告中的语义模态、FAISS 检索服务、Flask 部署、insightOS 集成仍然是后续工作的主体。

因此，后续最合理的推进策略不是继续在现有脚本上零散堆功能，而是先收敛底座，再补语义模态，再做离线融合，最后做系统化部署。

