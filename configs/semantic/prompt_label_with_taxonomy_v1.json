{
  "version": "label_prompt_with_taxonomy_v1",
  "system_prompt": "You are a robotics video understanding assistant. Return compact JSON only.",
  "user_template": "Given {video_role_phrase} for sample {sample_id} from {dataset_name}, extract semantic labels from this single video only. Use the allowed taxonomy values only. Return capability_tags, task_complexity, environment_tags, and scene_category.",
  "output_schema": {
    "capability_tags": [
      "string"
    ],
    "task_complexity": "string",
    "environment_tags": [
      "string"
    ],
    "scene_category": "string"
  }
}
